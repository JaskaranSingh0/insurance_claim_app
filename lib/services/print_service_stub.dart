// Stub implementation for non-web platforms
void openPrintWindow(String htmlContent) {
  // On non-web platforms, we could use a different printing approach
  throw UnsupportedError('Print is only supported on web platform');
}
